<Window x:Class="Scrybe.App.Views.SecretPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:Scrybe.App.Localization"
        Title="{loc:Translate Secrets.PaletteTitle}"
        Height="380"
        Width="520"
        MinHeight="320"
        MinWidth="420"
        WindowStartupLocation="Manual"
        Background="{StaticResource BackgroundBrush}"
        ShowInTaskbar="False"
        UseLayoutRounding="True">

    <Window.Resources>
        <Style x:Key="PaletteEmptyTextStyle" TargetType="TextBlock" BasedOn="{StaticResource ManagerEmptyStateTextStyle}">
            <Setter Property="Visibility" Value="Collapsed" />
        </Style>
    </Window.Resources>

    <DockPanel Margin="{StaticResource SpacingLg}">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="{loc:Translate Secrets.Inject}"
                    Command="{Binding InjectCommand}"
                    IsEnabled="{Binding CanInject}"
                    IsDefault="True"
                    Padding="{StaticResource PaddingButtonPrimary}"
                    Background="{StaticResource AccentBrush}"
                    Foreground="{StaticResource ActionOnBrightBrush}"
                    BorderThickness="0"
                    FontWeight="SemiBold"
                    AutomationProperties.Name="{loc:Translate Secrets.Inject}" />
        </StackPanel>

        <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
            <TextBox x:Name="SearchBox"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Background="{StaticResource SurfaceBrush}"
                     Foreground="{StaticResource TextPrimaryBrush}"
                     CaretBrush="{StaticResource TextPrimaryBrush}"
                     BorderBrush="{StaticResource BorderBrush}"
                     Padding="{StaticResource PaddingInput}"
                     AutomationProperties.Name="{loc:Translate Palette.Search}" />
            <TextBlock Text="{loc:Translate Palette.SearchPlaceholder}"
                       Foreground="{StaticResource TextSecondaryBrush}"
                       Margin="10,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <Grid>
            <ListBox ItemsSource="{Binding Secrets}"
                     SelectedItem="{Binding SelectedSecret}"
                     Background="{StaticResource SurfaceBrush}"
                     Foreground="{StaticResource TextPrimaryBrush}"
                     BorderBrush="{StaticResource BorderBrush}"
                     BorderThickness="1"
                     ScrollViewer.CanContentScroll="True"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     FontSize="{StaticResource FontSizeBody}"
                     AutomationProperties.Name="{loc:Translate Shell.TabSecrets}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,4">
                            <TextBlock Text="{Binding Name}"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       FontSize="{StaticResource FontSizeBodyLarge}"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="{Binding UserName}"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       FontSize="{StaticResource FontSizeCaption}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <TextBlock Text="{loc:Translate Secrets.Empty}">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource PaletteEmptyTextStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasSecrets}" Value="False">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBlock Text="{loc:Translate Secrets.NoMatches}">
                <TextBlock.Style>
                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource PaletteEmptyTextStyle}">
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasSecrets}" Value="True" />
                                    <Condition Binding="{Binding IsEmpty}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>
    </DockPanel>
</Window>
